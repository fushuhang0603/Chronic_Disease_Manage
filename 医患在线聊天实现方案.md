# 医患在线聊天 — 实现方案

## 一、整体架构

```
前端(Vue3) ──HTTP──→ Gateway(9000) ──→ health-record-service (REST: 历史消息、标记已读)
    │                                       │
    └──WebSocket──→ Gateway(9000) ──────────┘ (实时消息收发)
                                                    │
                                                    ├── MySQL (永久存储)
                                                    └── Redis (在线状态 + 未读计数)
```

**职责分工：**

| 组件 | 职责 |
|------|------|
| MySQL | 消息历史，永久存储 |
| Redis | 在线状态 (TTL 60s)、未读计数缓存、WebSocket 连接信息 |
| WebSocket | 实时推送通道，毫秒级触达 |
| REST API | 历史消息分页、标记已读、医生患者列表 |

---

## 二、数据表

```sql
CREATE TABLE consultation_record (
  id            BIGINT        NOT NULL AUTO_INCREMENT COMMENT '消息ID',
  patient_id    BIGINT        NOT NULL COMMENT '患者用户ID',
  patient_name  VARCHAR(50)   NOT NULL COMMENT '患者姓名',
  doctor_id     BIGINT        NOT NULL COMMENT '医生用户ID',
  doctor_name   VARCHAR(50)   NOT NULL COMMENT '医生姓名',
  sender_id     BIGINT        NOT NULL COMMENT '发送人用户ID',
  sender_role   VARCHAR(10)   NOT NULL COMMENT '发送人角色: PATIENT / DOCTOR',
  content       TEXT          NOT NULL COMMENT '消息内容',
  is_read       TINYINT       DEFAULT 0 COMMENT '是否已读: 0未读, 1已读',
  is_deleted    TINYINT       DEFAULT 0 COMMENT '逻辑删除: 0正常, 1已删除',
  create_time   DATETIME      DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  create_user   BIGINT        DEFAULT NULL COMMENT '创建人',
  update_time   DATETIME      DEFAULT NULL ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  update_user   BIGINT        DEFAULT NULL COMMENT '更新人',
  PRIMARY KEY (id),
  INDEX idx_patient_doctor (patient_id, doctor_id),
  INDEX idx_create_time (create_time),
  INDEX idx_is_read (is_read)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='医患沟通记录表';
```

**为什么姓名直接落库？**
- 问诊消息量大，每次查历史都跨服务反查姓名性能差
- 聊天场景下姓名变了也不影响历史消息展示

---

## 三、Redis Key 设计

| Key | 类型 | 值 | TTL | 用途 |
|-----|------|-----|-----|------|
| `online:{userId}` | String | `1` | 60s | 标记用户在线，心跳续期 |
| `ws:{userId}` | Hash | `{sessionId}` | — | WebSocket 连接信息 |
| `unread:{userId}` | Hash | `{patientId: N}` | — | 缓存每个聊天对象的未读数 |

---

## 四、消息收发流程

### 4.1 发送消息

```
患者点「发送」
      │
      ▼ WebSocket
┌─────────────────────────────────────┐
│ 后端 ChatWebSocketHandler.onMessage │
│                                     │
│ ① 校验: userId == patientId        │
│ ② Feign 验绑定关系                  │
│ ③ Feign 查姓名                      │
│ ④ 存 MySQL (consultation_record)   │
│ ⑤ HINCRBY unread:{doctorId} {patientId} 1 │
│ ⑥ EXISTS online:{doctorId}         │
│     → 在线: WebSocket 推送          │
│     → 离线: 跳过，消息已在 MySQL 中  │
└─────────────────────────────────────┘
```

### 4.2 接收消息

```
WebSocket 推送消息到达医生前端
      │
      ├─ 医生正在和张三聊天，消息来自张三
      │    → 直接插入当前聊天窗口，滚动到底部
      │
      ├─ 医生正在和张三聊天，消息来自李四
      │    → 右上角 Toast: "李四发来新消息"
      │    → 侧边栏「患者列表」菜单显示红点
      │    → 不插入当前聊天窗口
      │
      └─ 医生在患者列表页
           → 更新李四的卡片: 新消息预览 + 未读数红点
```

### 4.3 加载历史消息

```
进入聊天页
      │
      ▼
REST GET /consultation/page?patientId=X&doctorId=Y&pageNum=1&pageSize=20
      │
      ▼
从 MySQL 查最近 20 条消息，按时间正序
      │
      ▼
前端渲染历史消息
      │
      ▼
建立 WebSocket，接收新消息
```

### 4.4 标记已读

```
医生点进张三的聊天页
      │
      ▼
REST PUT /consultation/read?patientId=X&doctorId=Y
      │
      ▼
UPDATE consultation_record SET is_read=1
WHERE patient_id=X AND doctor_id=Y
  AND sender_role='PATIENT' AND is_read=0
      │
      ▼
HDEL unread:{doctorId} X  (清空该患者的未读缓存)
```

---

## 五、在线状态管理

### 5.1 用户上线

```
WebSocket 连接建立 (onOpen)
      │
      ├─ 解析 JWT → userId
      ├─ SET online:{userId} 1 EX 60
      ├─ 存 ws:{userId}
      └─ 查 unread:{userId} → 推未读数给前端
```

### 5.2 心跳保活

```
前端 ── 每 30 秒 Ping ──→ 后端
                            EXPIRE online:{userId} 60
```

如果 60 秒内没有心跳，Redis Key 自动过期，判为离线。

### 5.3 用户下线

```
WebSocket 断开 (onClose)
      │
      ├─ DEL online:{userId}
      └─ DEL ws:{userId}
```

未读缓存不删，下次上线还能看到红点。

---

## 六、医生界面设计

### 6.1 布局 (LayoutDoctor.vue)

```
┌────────────────────────────────────────────┐
│  慢病健康管理 — 医生端           张医生 退出 │  顶栏
├──────────┬─────────────────────────────────┤
│ 🏥       │                                 │
│ 患者列表  │        <router-view />          │  主区域
│  🔴 1    │                                 │
│          │                                 │
└──────────┴─────────────────────────────────┘
       200px             剩余
```

### 6.2 患者列表页 (DoctorPatients.vue)

```
┌──────────────────────────────────────────────┐
│  我的患者                          共 3 位     │
├──────────────────────────────────────────────┤
│  ┌────┐                                     │
│  │ 张 │ 张三  🟢 在线             🔴 未读 3  │
│  │    │ 最近血压控制得不错...       10:30     │
│  └────┘                                     │
│  ┌────┐                                     │
│  │ 李 │ 李四  ⚫ 离线                       │
│  │    │ 请问药需要调整吗？           昨天     │
│  └────┘                                     │
└──────────────────────────────────────────────┘
```

在线绿点/离线灰点来自 `EXISTS online:{patientId}`。

### 6.3 聊天页 (DoctorConsultation.vue)

```
┌──────────────────────────────────────────────┐
│  ← 返回    张三  🟢 在线                     │  顶栏(固定)
├──────────────────────────────────────────────┤
│                    7月25日                    │  时间分隔
│  ┌──────────────────────────┐                │
│  │ 张医生您好，血压有些不稳   │ ← 患者(左白底) │
│  └──────────────────────────┘                │
│             ┌──────────────────────────┐    │
│             │ 看到记录了，注意饮食清淡   │ ← 医生(右暖色)│
│             └──────────────────────────┘    │
│          ...消息区可滚动...                   │
├──────────────────────────────────────────────┤
│  [输入消息...]                          [发送] │  底部(固定)
└──────────────────────────────────────────────┘
```

---

## 七、后端 API

### 7.1 REST API (ConsultationController)

| 方法 | 路径 | 用途 |
|------|------|------|
| POST | `/consultation/page` | 分页查历史消息 |
| PUT | `/consultation/read` | 标记已读 |
| GET | `/consultation/doctor/patients` | 医生患者列表 |

### 7.2 WebSocket

| 通道 | 方向 | 内容 |
|------|------|------|
| `ws://gateway/consultation-ws` | 双向 | JSON: `{patientId, doctorId, content, senderRole, createTime}` |

### 7.3 DTO 设计

```
ConsultationMessageDTO:
  patientId (Long)   — 前端传
  doctorId  (Long)   — 前端传
  content   (String) — 前端传
  senderRole: 后端从 UserInfoContext.getRole() 取，前端不需要传

ConsultationPageDTO:
  patientId (Long)
  doctorId  (Long)
  pageNum   (Integer, 默认 1)
  pageSize  (Integer, 默认 20)
```

---

## 八、基础设施改造

### 8.1 网关 AuthGlobalFilter
原只传 `user-info` (userId)，新增传递 `user-role` (role)，空值保护。

### 8.2 UserInfoContext
新增 `ThreadLocal<String> ROLE`，提供 `setRole/getRole/clear`。

### 8.3 UserInfoInterceptor
从 `user-role` header 读取角色存入 UserInfoContext。

### 8.4 网关路由
```yaml
- id: consultation-ws
  uri: lb://health-record-service
  predicates:
    - Path=/consultation-ws/**
- id: consultation-record
  uri: lb://health-record-service
  predicates:
    - Path=/consultation/**
```

---

## 九、跨服务调用

### 9.1 DoctorPatientFeign (新增)
```java
@FeignClient(name = "health-user-service", path = "/doctor/patient")
- getMyDoctor()      → GET /my          (患者查绑定医生，已有)
- getMyPatients()    → GET /myPatients  (医生查绑定患者，需新增)
```

### 9.2 UserServiceFeign (修改)
```java
- getUserById(id)    → GET /user/queryById  (查用户姓名，新增)
```

### 9.3 user-service 新增接口
```java
GET /doctor/patient/myPatients  → 返回该医生的所有绑定患者列表
```

---

## 十、改动文件清单

### 后端 (12 个文件)

| 模块 | 文件 | 操作 |
|------|------|------|
| gateway | `AuthGlobalFilter.java` | 修改 (传 role) |
| common | `UserInfoContext.java` | 修改 (加 role) |
| common | `UserInfoInterceptor.java` | 修改 (读 role) |
| gateway | `application.yml` | 修改 (加路由) |
| record-service | `pom.xml` | 修改 (加 websocket + redis) |
| record-service | `WebSocketConfig.java` | 新增 |
| record-service | `ChatWebSocketHandler.java` | 新增 |
| record-service | `RedisConfig.java` | 新增 (从 article-service 搬) |
| record-service | `ConsultationRecord.java` | 新增 (Entity) |
| record-service | `ConsultationRecordMapper.java` | 新增 (Mapper) |
| record-service | `ConsultationMessageDTO.java` | 新增 |
| record-service | `ConsultationPageDTO.java` | 新增 |
| record-service | `DoctorPatientVO.java` | 新增 |
| record-service | `IConsultationService.java` | 新增 |
| record-service | `ConsultationServiceImpl.java` | 新增 |
| record-service | `ConsultationController.java` | 新增 |
| record-service | `DoctorPatientFeign.java` | 新增 |
| record-service | `UserServiceFeign.java` | 修改 (加 getUserById) |
| user-service | `DoctorPatientController.java` | 修改 (加 /myPatients) |
| user-service | `IDoctorPatientService.java` + Impl | 修改 (加方法) |

### 前端 (8 个文件)

| 文件 | 操作 |
|------|------|
| `package.json` | 修改 (无需额外依赖，浏览器原生 WebSocket) |
| `api/user.js` | 修改 (加 API 函数) |
| `LayoutDoctor.vue` | 新增 |
| `DoctorPatients.vue` | 新增 |
| `DoctorConsultation.vue` | 新增 |
| `PatientConsultation.vue` | 新增 |
| `PatientDoctors.vue` | 修改 (加「在线咨询」按钮) |
| `router/index.js` | 修改 (加路由) |

---

## 十一、前端路由

```js
// 医生端
{ path: '/doctor', component: LayoutDoctor, children: [
  { path: 'patients', name: 'DoctorPatients', component: DoctorPatients },
  { path: 'consultation/:patientId', name: 'DoctorConsultation', component: DoctorConsultation },
]}

// 患者端
{ path: '/patient/consultation/:doctorId', name: 'PatientConsultation', component: PatientConsultation },
```

---

## 十二、消息不丢失保障

```
发送消息 → 第一步存 MySQL → 成功 → 第二步推 WebSocket
                            → 失败 → 报错返回

接收方离线 → 消息已在 MySQL → 下次上线从 REST /page 加载 → 未读数从 Redis 读

WebSocket 断开 → MySQL 历史全在 → 重新连接后恢复
```
