# 轻量慢病健康管理微服务系统 数据库设计开发文档
## 文档版本：V1.0
## 一、项目基础信息
### 1. 技术栈
+ 后端：SpringCloud Alibaba + SpringBoot3 + Nacos（注册 + 配置）+ Gateway + OpenFeign + Sentinel + JWT
+ 中间件：RocketMQ、Elasticsearch、ELK (Logstash+ES+Kibana)、Redis
+ 数据库：MySQL8.0，垂直分库，**无物理外键，全部逻辑关联**
+ 前端：Vue3 + Element Plus + ECharts

### 2. 分库规划

| **数据库名称** | **对应微服务** | **存储内容** |
| :--- | :--- | :--- |
| db_user | user-service 用户档案服务 | 用户、健康档案、术语字典 |
| db_health_record | health-record-service 健康数据服务 | 健康指标监测、用药记录、复查记录 |
| db_remind | remind-service 提醒医患交互服务 | 定时提醒、医生留言、站内通知 |
| db_article | article-service 健康资讯服务 | 慢病科普资讯、收藏、阅读历史 |

### 3. 数据库设计规范
1. 存储引擎：InnoDB，字符集 utf8mb4，排序规则 utf8mb4_unicode_ci
2. 统一软删除：所有表增加 `is_deleted tinyint DEFAULT 0`，0 未删除，1 已删除
3. 统一审计字段：`create_time` 创建时间、`update_time` 更新时间
4. 无物理外键约束，仅建立业务索引，数据一致性由代码层校验
5. 一人一角色设计，通过 `role_type` 区分用户身份，不使用复杂 RBAC 多角色

## 二、四大微服务完整说明
### 2.1 user-service 用户档案服务（对应库：db_user）
#### 核心职责
系统底层基础支撑服务，为所有微服务提供用户身份、档案、监测字典基础数据。

1. 用户注册、登录、JWT 令牌签发、密码 BCrypt 加密、退出登录
2. 用户头像、手机号、昵称修改，账号启用 / 禁用管理
3. 身份区分：1 患者、2 医生、3 健康管理员、4 超级管理员，一人仅能拥有一种角色
4. 患者健康档案新增、编辑、查询，支持绑定专属负责医生
5. 全局术语字典维护（指标/疾病/药品三类术语统一配置，供健康数据服务引用）
6. Redis 缓存登录信息、角色权限、指标字典，降低数据库查询压力

#### 对应数据表
sys_user、health_archive、term_dict

### 2.2 health-record-service 健康数据服务（对应库：db_health_record）
#### 核心职责
慢病核心业务服务，承载全部患者日常健康监测数据的录入、查询、统计分析与可视化展示。

**1. 健康指标监测管理**
- 统一管理全部健康指标（血压、血糖、血脂、尿酸、BMI、腰围等），不区分身体指标与生化指标
- 指标编码通过 term_code 关联术语字典表（term_type=indicator），实现指标名称统一管理
- 支持单条录入、批量导入、分页查询（按指标/时间范围筛选）
- 同一用户同一天可多次测量同一指标并全部留存

**2. 长期用药管理**
- 药品编码通过 drug_code 关联术语字典（term_type=medicine），药品名称随字典统一维护
- 记录服用剂量、频次、开始日期、停药日期（停药日期为空表示持续服用中）
- 按用药状态筛选（服药中/已停药）

**3. 复查记录管理**
- 复查项目编码通过 term_code 关联术语字典，每项复查单独一条记录，支持按项目统计
- 记录医院名称、检查结果、医生诊断建议、实际复查日期、计划下次复查日期

**4. 趋势分析与可视化**
- 按日/周/月维度统计指定指标的平均值、最高值、最低值
- 对外提供 ECharts 图表数据接口，前端渲染折线图趋势
- 支持多指标同时对比展示（如收缩压+舒张压同一图表）

**5. 异步业务处理**
- 新增监测数据通过 OpenFeign 调用 user-service 校验患者身份、校验指标编码合法性
- RocketMQ 异步消息：数据录入后发送 MQ，由消费者完成每日健康报告汇总、异常标记归档

#### 对应数据表
health_index_record（通用指标记录）、medicine_record（用药记录）、recheck_record（复查记录）

### 2.3 remind-service 智能提醒与医患交互服务（对应库：db_remind）
#### 核心职责
智能提醒中枢，结合患者健康数据自动生成个性化提醒，不再依赖手动创建提醒计划。同时承载医患双向沟通与站内消息推送。

**1. 智能健康提醒引擎**
- 消费 health-record-service 的 MQ 消息，自动分析最新监测数据：
  - 指标数据录入后，对比历史趋势判断是否波动异常 → 自动生成异常告警通知
  - 新增用药记录 → 根据频次自动生成吃药提醒计划
  - 复查记录 plan_next_time 到期前 3 天 → 自动生成复查提醒
- 患者也可手动创建自定义提醒（运动、饮水、自定义事项）
- 定时任务每 5 分钟扫描到期提醒，推送 MQ 生成站内通知

**2. 健康周报自动推送**
- 每周一自动拉取 health-record-service 上周全部指标数据
- 生成汇总周报（各指标平均值、用药依从率、复查完成情况）
- MQ 异步推送周报到患者站内通知

**3. 医患双向互动**
- 医生向绑定患者下发养护叮嘱、用药调整建议
- 患者可回复医生留言，形成留言线程
- 留言已读/未读状态实时追踪

**4. 消息中心统一管理**
- 全部消息分四类：系统通知、健康提醒、医生留言、异常告警
- 支持按类别筛选、批量已读、一键全部已读
- 未读消息红点提醒

**5. 跨服务协作**
- OpenFeign 调用 user-service：校验医患绑定关系、获取患者档案（慢病类型用于提醒文案定制）
- OpenFeign 调用 health-record-service：拉取指标趋势数据生成提醒内容、查询用药列表、查询复查计划
- MQ 消费 health-record-service 数据变更消息，触发智能提醒生成

#### 对应数据表
health_remind（提醒计划）、doctor_message（医患留言）、sys_notice（站内通知）

### 2.4 article-service 智能资讯推荐服务（对应库：db_article）
#### 核心职责
慢病科普资讯管理、ES 全文检索、个性化智能推荐、用户阅读行为追踪。

**1. 资讯全生命周期管理**
- 后台管理员对资讯新增、编辑、上下架、删除
- 资讯新增/修改/删除时自动同步 Elasticsearch 索引
- 资讯分类管理：饮食、运动、用药、慢病常识、并发症预防

**2. 个性化智能推荐**
- 根据患者档案 chronic_type（慢病类型）智能匹配相关资讯
- 基于用户阅读历史的协同过滤推荐
- 热门资讯排行榜（ES 聚合浏览量，近 7 天、近 30 天）
- OpenFeign 调用 user-service 获取患者档案（慢病类型、年龄、性别）用于推荐策略

**3. 全文检索与收藏**
- Elasticsearch 分词检索：按标题、内容、分类多条件组合检索
- 高亮关键词匹配结果
- 用户收藏文章、取消收藏、我的收藏列表
- 阅读历史记录自动保存

**4. 缓存策略**
- Redis 缓存首页热门资讯 TOP 10，TTL 30 分钟
- Redis 缓存患者个性化推荐列表，用户档案更新时主动失效
- ES 查询结果 Redis 二级缓存

**5. 跨服务协作**
- OpenFeign 调用 user-service：校验管理员身份权限（新增/编辑/删除资讯）、获取患者档案用于个性化推荐

#### 对应数据表
health_article（资讯）、新增：article_favorite（收藏）、article_read_history（阅读历史）

### 2.5 OpenFeign 跨服务调用关系矩阵

| **调用方** | **被调用方** | **调用方法** | **调用场景** |
| :--- | :--- | :--- | :--- |
| health-record-service | user-service | checkPatientExist(userId) | 录入指标/用药/复查时校验患者身份 |
| health-record-service | user-service | getTermByCode(code) | 校验指标编码、药品编码、复查项目编码合法性 |
| health-record-service | user-service | getPatientArchive(userId) | 获取患者档案信息辅助数据录入 |
| health-record-service | remind-service | sendAbnormalAlert(dto) | 指标异常波动时通知提醒服务生成告警 |
| remind-service | user-service | checkDoctorBind(doctorId, patientId) | 医生留言前校验医患绑定关系 |
| remind-service | user-service | getPatientArchive(userId) | 获取慢病类型用于定制提醒文案 |
| remind-service | user-service | getDoctorInfo(doctorId) | 获取医生信息展示在留言中 |
| remind-service | health-record-service | getLatestIndicators(userId, days) | 生成健康周报时拉取近 7 天指标数据 |
| remind-service | health-record-service | getMedicineList(userId) | 生成用药提醒时拉取当前服用药品列表 |
| remind-service | health-record-service | getComingRechecks(userId, days) | 生成复查提醒时拉取 3 天内到期复查计划 |
| article-service | user-service | checkAdminRole(userId) | 资讯增删改操作校验管理员身份 |
| article-service | user-service | getPatientArchive(userId) | 获取患者慢病类型用于个性化推荐 |

## 三、完整分库建表 SQL 语句（无物理外键，可直接执行）
### 3.1 数据库 db_user（user-service）
```plain
CREATE DATABASE IF NOT EXISTS db_user DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
USE db_user;

-- 1. 用户表 sys_user
CREATE TABLE `sys_user` (
  `id` bigint NOT NULL AUTO_INCREMENT COMMENT '用户主键ID',
  `username` varchar(30) NOT NULL COMMENT '登录账号，唯一',
  `password` varchar(100) NOT NULL COMMENT 'BCrypt加密密码',
  `nickname` varchar(30) NOT NULL COMMENT '用户昵称',
  `avatar` varchar(255) DEFAULT '' COMMENT '头像图片地址',
  `phone` varchar(11) DEFAULT '' COMMENT '手机号',
  `role_type` tinyint NOT NULL DEFAULT 1 COMMENT '角色：1患者 2医生 3健康管理员 4超级管理员',
  `status` tinyint DEFAULT 1 COMMENT '账号状态：0禁用 1正常',
  `last_login_time` datetime DEFAULT NULL COMMENT '最后登录时间',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `is_deleted` tinyint DEFAULT 0 COMMENT '逻辑删除：0未删 1已删',
  PRIMARY KEY (`id`),
  UNIQUE KEY uk_username (`username`,`is_deleted`),
  KEY idx_role (`role_type`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='系统用户表';

-- 2. 健康档案表 health_archive
CREATE TABLE `health_archive` (
  `id` bigint NOT NULL AUTO_INCREMENT COMMENT '档案主键ID',
  `user_id` bigint NOT NULL COMMENT '绑定患者用户ID，一人一档唯一',
  `age` int DEFAULT 0 COMMENT '年龄',
  `gender` tinyint DEFAULT 0 COMMENT '0未知 1男 2女',
  `chronic_type` varchar(100) DEFAULT '' COMMENT '慢病类型，逗号分隔：高血压,糖尿病,痛风',
  `medical_history` varchar(500) DEFAULT '' COMMENT '既往病史',
  `allergy_history` varchar(500) DEFAULT '' COMMENT '药物/食物过敏史',
  `life_habit` varchar(500) DEFAULT '' COMMENT '吸烟、饮酒、运动习惯',
  `bind_doctor_id` bigint DEFAULT NULL COMMENT '绑定专属负责医生ID',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP,
  `update_time` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `is_deleted` tinyint DEFAULT 0,
  PRIMARY KEY (`id`),
  UNIQUE KEY uk_user_id (`user_id`,`is_deleted`),
  KEY idx_doctor (`bind_doctor_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='患者健康档案表';

-- 3. 术语字典表 term_dict
CREATE TABLE `term_dict` (
  `id` bigint NOT NULL AUTO_INCREMENT COMMENT '术语ID',
  `term_code` varchar(50) NOT NULL COMMENT '术语编码（英文唯一标识）',
  `term_name` varchar(100) NOT NULL COMMENT '术语名称（中文）',
  `term_type` varchar(50) NOT NULL DEFAULT 'indicator' COMMENT '术语类型：indicator指标 / disease疾病 / medicine药品',
  `sort` int DEFAULT 0 COMMENT '排序权重，同类型内排序',
  `status` tinyint DEFAULT 1 COMMENT '状态：0停用 1启用',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `is_deleted` tinyint DEFAULT 0 COMMENT '逻辑删除：0未删 1已删',
  PRIMARY KEY (`id`),
  UNIQUE KEY uk_term_code (`term_code`),
  KEY idx_term_type (`term_type`),
  KEY idx_deleted_type_sort (`is_deleted`, `term_type`, `sort`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='术语字典表';
```

> 设计说明：term_dict 统一管理指标（indicator）、疾病（disease）、药品（medicine）三类术语。健康监测记录通过 index_code 关联指标类术语，用药记录通过 drug_code 关联药品类术语，复查记录通过 recheck_item_code 关联指标/疾病类术语。term_type 字段区分术语类型，同一张表覆盖全部业务场景，避免多表维护。复合索引 idx_deleted_type_sort 覆盖 is_deleted 过滤 + term_type 分组 + sort 排序，一次查全无 filesort。

### 3.2 数据库 db_health_record（health-record-service）

```sql
CREATE DATABASE IF NOT EXISTS db_health_record DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
USE db_health_record;

-- 1. 健康指标监测记录表 health_index_record
CREATE TABLE `health_index_record` (
  `id` bigint NOT NULL AUTO_INCREMENT COMMENT '记录ID',
  `user_id` bigint NOT NULL COMMENT '患者用户ID',
  `index_code` varchar(50) NOT NULL COMMENT '指标编码，关联术语字典 term_dict.term_code（term_type=indicator）',
  `index_value` decimal(6,2) NOT NULL COMMENT '指标数值',
  `record_time` datetime NOT NULL COMMENT '测量时间',
  `remark` varchar(200) DEFAULT '' COMMENT '备注：测量场景、身体状态',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `is_deleted` tinyint DEFAULT 0 COMMENT '逻辑删除：0未删 1已删',
  PRIMARY KEY (`id`),
  KEY idx_user_index_time (`user_id`, `index_code`, `record_time`),
  KEY idx_user_time (`user_id`, `record_time`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='健康指标监测记录表';
```

> 设计说明：统一存储全部健康指标（血压、血糖、血脂、尿酸、BMI、体重、腰围、心率等），不再单独拆分身体指标表。index_code 逻辑关联 term_dict（term_type=indicator），指标名称和编码统一由术语字典维护。复合索引 idx_user_index_time 覆盖用户+指标+时间的联合查询（最常用的趋势分析场景），idx_user_time 覆盖用户时间线浏览。指标异常判断由业务层根据固定阈值实现，不在数据库存储。

```sql
-- 2. 用药记录表 medicine_record
CREATE TABLE `medicine_record` (
  `id` bigint NOT NULL AUTO_INCREMENT COMMENT '用药记录ID',
  `user_id` bigint NOT NULL COMMENT '患者用户ID',
  `drug_code` varchar(50) NOT NULL COMMENT '药品编码，关联术语字典 term_dict.term_code（term_type=medicine）',
  `dosage` varchar(50) NOT NULL COMMENT '服用剂量，如：0.5g/次、1片/次',
  `frequency` varchar(50) NOT NULL COMMENT '服用频次，如：每日2次、早晚各1片',
  `start_date` date NOT NULL COMMENT '开始服药日期',
  `stop_date` date DEFAULT NULL COMMENT '停药日期，为空表示持续服用中',
  `remark` varchar(200) DEFAULT '' COMMENT '医嘱备注、不良反应记录',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `is_deleted` tinyint DEFAULT 0 COMMENT '逻辑删除：0未删 1已删',
  PRIMARY KEY (`id`),
  KEY idx_user_drug (`user_id`, `drug_code`),
  KEY idx_user_status (`user_id`, `stop_date`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='患者用药记录表';
```

> 设计说明：drug_code 逻辑关联 term_dict（term_type=medicine），药品名称随术语字典统一维护，前端展示时通过 drug_code JOIN 或缓存获取 drug_name。用药状态由 stop_date 隐式判断（NULL=服药中，有值=已停药），无需额外状态字段。索引 idx_user_drug 支持按用户+药品筛选，idx_user_status 支持按用药状态（停药时间）筛选。

```sql
-- 3. 复查记录表 recheck_record
CREATE TABLE `recheck_record` (
  `id` bigint NOT NULL AUTO_INCREMENT COMMENT '复查记录ID',
  `user_id` bigint NOT NULL COMMENT '患者用户ID',
  `hospital_name` varchar(100) NOT NULL COMMENT '复查医院名称',
  `recheck_item_code` varchar(50) NOT NULL COMMENT '复查项目编码，关联术语字典 term_dict.term_code（term_type=indicator 或 disease）',
  `recheck_result` text COMMENT '检查结果描述',
  `doctor_advice` text COMMENT '医生诊断建议',
  `actual_recheck_time` date NOT NULL COMMENT '实际复查日期',
  `plan_next_time` date DEFAULT NULL COMMENT '计划下次复查日期',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `is_deleted` tinyint DEFAULT 0 COMMENT '逻辑删除：0未删 1已删',
  PRIMARY KEY (`id`),
  KEY idx_user_time (`user_id`, `actual_recheck_time`),
  KEY idx_user_item (`user_id`, `recheck_item_code`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='线下复查记录表';
```

> 设计说明：recheck_item_code 逻辑关联 term_dict，每项复查单独一条记录（一次复查多个项目则产生多条记录），避免逗号拼接字符串无法统计的问题。索引 idx_user_time 支持按时间线查看复查历史，idx_user_item 支持按复查项目统计趋势。

### 3.3 数据库 db_remind（remind-service）
```plain
CREATE DATABASE IF NOT EXISTS db_remind DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
USE db_remind;

-- 1. 提醒计划表 health_remind
CREATE TABLE `health_remind` (
  `id` bigint NOT NULL AUTO_INCREMENT COMMENT '提醒计划ID',
  `user_id` bigint NOT NULL COMMENT '所属用户ID',
  `remind_type` tinyint NOT NULL DEFAULT 1 COMMENT '1吃药 2运动 3复查',
  `remind_content` varchar(200) NOT NULL COMMENT '提醒文字内容',
  `remind_time` time NOT NULL COMMENT '每日提醒时刻',
  `status` tinyint DEFAULT 1 COMMENT '0关闭 1启用',
  `last_push_time` datetime DEFAULT NULL COMMENT '上次消息推送时间',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP,
  `update_time` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `is_deleted` tinyint DEFAULT 0,
  PRIMARY KEY (`id`),
  KEY idx_user_status (`user_id`,`status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户健康提醒计划表';

-- 2. 医生留言表 doctor_message
CREATE TABLE `doctor_message` (
  `id` bigint NOT NULL AUTO_INCREMENT COMMENT '留言ID',
  `doctor_id` bigint NOT NULL COMMENT '留言医生用户ID',
  `patient_id` bigint NOT NULL COMMENT '接收患者用户ID',
  `content` text NOT NULL COMMENT '医生养护、用药叮嘱内容',
  `read_status` tinyint DEFAULT 0 COMMENT '0未读 1已读',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP,
  `update_time` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `is_deleted` tinyint DEFAULT 0,
  PRIMARY KEY (`id`),
  KEY idx_patient_read (`patient_id`,`read_status`),
  KEY idx_doctor (`doctor_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='医生患者留言叮嘱表';

-- 3. 站内通知表 sys_notice
CREATE TABLE `sys_notice` (
  `id` bigint NOT NULL AUTO_INCREMENT COMMENT '通知ID',
  `user_id` bigint NOT NULL COMMENT '接收消息用户ID',
  `msg_type` tinyint NOT NULL COMMENT '1吃药提醒 2运动提醒 3复查提醒 4医生留言',
  `title` varchar(100) NOT NULL COMMENT '消息标题',
  `content` varchar(255) NOT NULL COMMENT '消息简短内容',
  `relation_id` bigint DEFAULT NULL COMMENT '关联业务ID：提醒ID/留言ID',
  `read_status` tinyint DEFAULT 0 COMMENT '0未读 1已读',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP,
  `is_deleted` tinyint DEFAULT 0,
  PRIMARY KEY (`id`),
  KEY idx_user_read (`user_id`,`read_status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='站内消息通知表';
```

### 3.4 数据库 db_article（article-service）
```plain
CREATE DATABASE IF NOT EXISTS db_article DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
USE db_article;

-- 1. 健康资讯表 health_article
CREATE TABLE `health_article` (
  `id` bigint NOT NULL AUTO_INCREMENT COMMENT '资讯ID',
  `title` varchar(100) NOT NULL COMMENT '资讯标题',
  `content` text NOT NULL COMMENT '文章正文，同步ES分词检索',
  `category` varchar(20) NOT NULL COMMENT '分类：饮食/运动/用药/慢病常识/并发症预防',
  `view_count` int DEFAULT 0 COMMENT '浏览量',
  `status` tinyint DEFAULT 1 COMMENT '0下架 1正常展示',
  `publishing_time` datetime NOT NULL COMMENT '发布时间',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP,
  `update_time` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `is_deleted` tinyint DEFAULT 0,
  PRIMARY KEY (`id`),
  KEY idx_category_status (`category`,`status`,`is_deleted`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='慢病健康资讯表';

-- 2. 用户收藏表 article_favorite
CREATE TABLE `article_favorite` (
  `id` bigint NOT NULL AUTO_INCREMENT COMMENT '收藏ID',
  `user_id` bigint NOT NULL COMMENT '用户ID',
  `article_id` bigint NOT NULL COMMENT '资讯ID',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP COMMENT '收藏时间',
  `is_deleted` tinyint DEFAULT 0 COMMENT '逻辑删除：0未删 1已删（取消收藏）',
  PRIMARY KEY (`id`),
  UNIQUE KEY uk_user_article (`user_id`, `article_id`, `is_deleted`),
  KEY idx_user (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户文章收藏表';

-- 3. 阅读历史表 article_read_history
CREATE TABLE `article_read_history` (
  `id` bigint NOT NULL AUTO_INCREMENT COMMENT '阅读记录ID',
  `user_id` bigint NOT NULL COMMENT '用户ID',
  `article_id` bigint NOT NULL COMMENT '资讯ID',
  `read_duration` int DEFAULT 0 COMMENT '阅读时长（秒）',
  `read_time` datetime DEFAULT CURRENT_TIMESTAMP COMMENT '阅读时间',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP,
  `is_deleted` tinyint DEFAULT 0,
  PRIMARY KEY (`id`),
  KEY idx_user_time (`user_id`, `read_time`),
  KEY idx_article (`article_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户文章阅读历史表';
```

### 3.5 数据库间表关联总结

## 四、开发功能完整清单
### 4.1 公共基础模块（所有服务通用）
1. 统一全局返回封装、全局异常处理器
2. JWT 工具、Token 拦截校验器
3. Redis 工具类（缓存、分布式锁）
4. RocketMQ 生产者 / 消费者通用封装
5. Elasticsearch 工具类（增删改查、聚合检索）
6. Spring 定时任务通用封装
7. Mybatis-Plus 逻辑删除全局配置
8. ELK 日志规范、操作日志异步投递工具

### 4.2 user-service 开发内容
1. 用户注册、登录、退出、密码加密
2. 用户信息修改、头像上传、账号启用 / 禁用
3. JWT 签发、基于 role_type 接口权限拦截
4. 健康档案 CRUD、医生绑定逻辑
5. 术语字典后台管理：指标/疾病/药品三类术语统一维护、前端下拉查询
6. OpenFeign 对外提供用户、档案查询接口
7. Redis 缓存登录用户、字典数据

### 4.3 health-record-service 开发内容
1. 健康指标记录录入、批量导入、分页查询（按指标/时间范围筛选）
2. 指标趋势统计接口：日/周/月平均值、最高/最低值，配合 ECharts 前端渲染折线图
3. 多指标同图对比展示（同一时间段不同指标趋势线叠加）
4. 用药记录新增、编辑、删除、列表查询，按用药状态（服药中/已停药）筛选
5. 药品编码关联术语字典，前端通过 drug_code 获取药品中文名称展示
6. 复查记录 CRUD，每项复查独立一条记录，支持按项目统计复查频率
7. 数据录入后发送 MQ（指标数据变更、用药变更、复查变更三个 Topic）
8. OpenFeign 调用 user-service 校验患者身份、校验编码、获取患者档案
9. OpenFeign 调用 remind-service 推送异常告警（指标波动超过阈值时）

### 4.4 remind-service 开发内容
1. 消费 health-record-service MQ 消息，自动生成用药提醒、复查提醒、异常告警
2. 患者手动创建自定义提醒计划（运动、饮水等）
3. 定时任务每 5 分钟扫描到期提醒，推送 MQ 生成站内通知
4. 每周一自动生成健康周报（拉取近 7 天指标、用药依从率、复查完成情况）
5. 医生留言 CRUD、患者回复留言（留言线程）
6. 留言已读/未读标记、按患者/医生维度查询
7. 用户消息中心：分页、按类别筛选、批量已读、一键全部已读
8. 未读消息红点提醒（总数 + 分类计数）
9. OpenFeign 调用 user-service 校验医患绑定关系、获取患者档案、医生信息
10. OpenFeign 调用 health-record-service 拉取指标趋势数据、用药列表、复查计划

### 4.5 article-service 开发内容
1. 健康资讯 CRUD，资讯自动同步 ES 索引
2. 资讯上下架管理
3. 前台分类浏览、ES 全文检索（高亮匹配词）
4. ES 浏览量统计聚合：热门资讯排行榜（近 7 天 / 近 30 天）
5. Redis 缓存首页热门 TOP 10 + 个性化推荐列表
6. 个性化推荐：根据患者慢病类型 + 阅读历史推荐资讯
7. 用户收藏/取消收藏文章、我的收藏列表
8. 阅读历史自动记录、阅读时长统计
9. OpenFeign 调用 user-service 校验管理员身份、获取患者档案用于推荐策略

### 4.6 Gateway 网关开发内容
1. 微服务路由、跨域处理
2. 全局 JWT 鉴权拦截
3. Sentinel 限流、熔断降级配置
4. 请求日志统一打印

### 4.7 中间件配套开发
1. RocketMQ 消息幂等、失败重试逻辑
2. ELK 日志采集配置
3. ES 同步失败重试机制

## 五、数据库总表汇总

| **数据库** | **归属微服务** | **数据表** | **数量** |
| :--- | :--- | :--- | :--- |
| db_user | user-service | sys_user、health_archive、term_dict | 3 |
| db_health_record | health-record-service | health_index_record、medicine_record、recheck_record | 3 |
| db_remind | remind-service | health_remind、doctor_message、sys_notice | 3 |
| db_article | article-service | health_article、article_favorite、article_read_history | 3 |

合计：4 个数据库，共 12 张数据表


## 六、业务架构全景图

### 6.1 服务调用与数据流拓扑

```
┌─────────────────────────────────────────────────────────────────────┐
│                           用户端 / 管理端                             │
│          (Vue3 前端 → Gateway 网关 → Nacos 服务发现)                   │
└──────────┬─────────┬──────────────┬──────────────┬──────────────────┘
           │         │              │              │
     ┌─────▼──┐ ┌───▼────┐  ┌──────▼──────┐ ┌────▼─────┐
     │ user   │ │health  │  │   remind    │ │ article  │
     │service │ │record  │  │   service   │ │ service  │
     │        │ │service │  │             │ │          │
     │端口9001│ │端口待定│  │  端口待定   │ │ 端口待定  │
     └───┬────┘ └──┬──┬──┘  └──┬──┬───┬──┘ └────┬─────┘
         │         │  │         │  │   │         │
     ┌───▼──┐ ┌───▼──▼──┐ ┌──▼──▼───▼──┐ ┌───▼─────┐
     │db_user│ │db_health│ │ db_remind  │ │db_article│
     │       │ │_record  │ │            │ │         │
     │3 张表 │ │3 张表   │ │ 3 张表     │ │3 张表    │
     └───────┘ └─────────┘ └────────────┘ └──────────┘

          ◄─── OpenFeign 远程调用 ───►
          ◄═══ RocketMQ 异步消息 ═══►
```

### 6.2 OpenFeign 调用关系图

```
                    ┌──────────────┐
          ┌────────►  user-service │◄──────────────┐
          │         │              │                │
          │         │  - 患者校验   │                │
          │         │  - 编码校验   │                │
          │         │  - 档案查询   │                │
          │         │  - 医患绑定   │                │
          │         │  - 管理员校验 │                │
          │         │  - 医生信息   │                │
          │         └─────────┬────┘                │
          │                   │                     │
          │                   │ (被所有服务调用)      │
          │                   │                     │
    ┌─────┴──────────┐       │        ┌─────────────┴──────┐
    │ health-record  │       │        │  remind-service    │
    │    -service    │       │        │                    │
    │                │       │        │  ┌────────────────┐│
    │ 调用 user:     │       │        │  │ 消费 MQ 消息:  ││
    │ · checkPatient │       │        │  │ 指标变更 → 告警 ││
    │ · getTermCode  │       │        │  │ 用药变更 → 提醒 ││
    │ · getArchive   │       │        │  │ 复查变更 → 提醒 ││
    │                │       │        │  └────────────────┘│
    │ 调用 remind:   ├───────┼───────►│                    │
    │ · sendAlert    │       │        │  调用 user:        │
    │                │       │        │  · checkBind       │
    └────────────────┘       │        │  · getArchive      │
                             │        │  · getDoctorInfo   │
    ┌────────────────┐       │        │                    │
    │ article-service│       │        │  调用 health:      │
    │                │       │        │  · getIndicators   │
    │ 调用 user:     ├───────┘        │  · getMedicines    │
    │ · checkAdmin   │                │  · getRechecks     │
    │ · getArchive   │                └────────────────────┘
    └────────────────┘
```

### 6.3 核心业务流程时序图

**场景：患者录入血压 → 异常波动 → 自动告警 → 医生查看留言**

```
患者端                  Gateway         health-record-    MQ Broker       remind-         user-
(Vue)                   (9000)          service           (RocketMQ)      service         service
  │                        │                │                │              │              │
  │ POST /api/record/add   │                │                │              │              │
  │───血压录入请求──────────►│                │                │              │              │
  │                        │──转发──────────►│                │              │              │
  │                        │                │──checkPatient───┼──────────────┼──────────────►│
  │                        │                │◄──患者存在──────┼──────────────┼───────────────│
  │                        │                │──getTermCode────┼──────────────┼──────────────►│
  │                        │                │◄──编码合法──────┼──────────────┼───────────────│
  │                        │                │                │              │              │
  │                        │                │──入库 health_index_record                 │
  │                        │                │                │              │              │
  │                        │                │──对比前 7 天数据，波动 > 20%               │
  │                        │                │                │              │              │
  │                        │                │──发送 MQ───────►│              │              │
  │                        │                │(TOPIC:HEALTH_ALERT)            │              │
  │                        │                │                │              │              │
  │                        │                │                │──推送给──────►│              │
  │                        │◄──录入成功──────│                │ consumer     │              │
  │◄──返回成功──────────────│                │                │              │              │
  │                        │                │                │              │              │
  │                        │                │                │   remind-service 消费消息    │
  │                        │                │                │              │              │
  │                        │                │                │              │──getArchive──►│
  │                        │                │                │              │◄──患者档案────│
  │                        │                │                │              │              │
  │                        │                │                │              │──自动生成告警 │
  │                        │                │                │              │   + 站内通知  │
  │                        │                │                │              │              │
  │                        │                │                │              │  写 doctor_   │
  │                        │                │                │              │  message      │
  │                        │                │                │              │  (留言提醒)   │
  │                        │                │                │              │              │
  │                        │                │                │              │  写 sys_notice│
  │                        │                │                │              │  (站内通知)   │
  │                        │                │                │              │              │
                          (异步完成，患者下次刷新看到告警)
```

### 6.4 数据流汇总

| **业务场景** | **数据流路径** | **通信方式** |
| :--- | :--- | :--- |
| 患者录入健康指标 | Vue → Gateway → health-record-service → MySQL | HTTP |
| 指标数据校验 | health-record-service → user-service | OpenFeign |
| 指标异常自动告警 | health-record-service → MQ → remind-service → 生成通知 | MQ 异步 |
| 智能用药提醒 | remind-service → health-record-service（拉取药品列表） | OpenFeign |
| 健康周报生成 | remind-service → health-record-service（拉取 7 天数据） | OpenFeign |
| 医生留言给患者 | Vue → Gateway → remind-service → MQ → 站内通知 | HTTP + MQ |
| 患者回复医生 | Vue → Gateway → remind-service → 站内通知 | HTTP |
| 医患关系校验 | remind-service → user-service | OpenFeign |
| 个性化资讯推荐 | Vue → Gateway → article-service → user-service（获取档案） | HTTP + OpenFeign |
| 资讯全文搜索 | Vue → Gateway → article-service → Elasticsearch | HTTP |
| 用户文章收藏 | Vue → Gateway → article-service → MySQL | HTTP |

